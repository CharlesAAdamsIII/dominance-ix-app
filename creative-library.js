(function(){
 'use strict';
 const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 let state=null;
 async function api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d}
 function textPreview(a){if(!a.text_content)return'';let d=null;try{d=JSON.parse(a.text_content)}catch{};const vars=d?.variants||[];if(vars.length)return vars.slice(0,3).map((v,i)=>`<div style="border-top:${i?'1px solid #1d303a':'0'};padding:${i?'8px 0 0':'0'};margin-top:${i?'8px':'0'}"><b>${esc(v.headline||'Variant')}</b><div style="color:#9db1bd;margin-top:4px">${esc(v.body_copy||'')}</div><div style="margin-top:5px;color:#5de0c2">CTA: ${esc(v.cta||'—')}</div>${v.rationale?`<div style="margin-top:5px;color:#c6ad5f">Why: ${esc(v.rationale)}</div>`:''}</div>`).join('');return `<pre style="white-space:pre-wrap;font:10px/1.45 Arial;color:#9db1bd">${esc(a.text_content.slice(0,1400))}</pre>`}
 function assetCard(a){const media=a.mime_type?.startsWith('image/')&&a.content_url?`<img src="${a.content_url}" style="width:100%;max-height:240px;object-fit:cover;border-radius:8px;border:1px solid #263946">`:a.mime_type?.startsWith('video/')&&a.content_url?`<video src="${a.content_url}" controls style="width:100%;max-height:260px;border-radius:8px;border:1px solid #263946"></video>`:'';const status=String(a.status||'').toUpperCase().replaceAll('_',' ');return `<div style="border:1px solid #263946;border-radius:10px;background:#09161e;padding:11px"><div style="display:flex;justify-content:space-between;gap:8px"><div><b>${esc(a.platform||'Platform')}</b><div style="font-size:9px;color:#78909d">${esc(a.asset_category)} · ${esc(a.asset_format||'')}</div></div><span style="font-size:8px;color:${a.status==='approved'?'#5de0c2':'#f3ce62'}">${esc(status)}</span></div>${media?`<div style="margin-top:8px">${media}</div>`:''}${a.text_content?`<div style="margin-top:8px">${textPreview(a)}</div>`:''}<div style="margin-top:8px;font-size:9px;color:#8195a3">Market: ${esc(a.market||'not specified')} · Angle: ${esc(a.message_angle||'research-led')}</div><div style="display:flex;gap:6px;margin-top:9px">${a.status==='approved'?'<span style="color:#5de0c2;font-size:9px">READY IN CAMPAIGN MANAGER</span>':`<button data-approve="${a.id}" style="border:1px solid #5de0c2;background:#17352d;color:#dff9f0;border-radius:7px;padding:6px 8px;font-size:9px;font-weight:800;cursor:pointer">Approve + Send to Campaign Manager</button><button data-reject="${a.id}" style="border:1px solid #5b3434;background:#231616;color:#ffb2b2;border-radius:7px;padding:6px 8px;font-size:9px;cursor:pointer">Reject</button>`}</div></div>`}
 function ensure(){let el=document.getElementById('domCreativeLibrary');if(el)return el;el=document.createElement('section');el.id='domCreativeLibrary';el.className='card';el.style.marginTop='14px';const perf=document.getElementById('performance');perf?.parentNode?.insertBefore(el,perf);return el}
 function render(){const el=ensure();if(!state){el.innerHTML='<div class="eyebrow" style="margin:0">RESEARCH + READY-TO-LAUNCH CREATIVE CMS</div><div class="brief" style="margin-top:8px">Loading evidence-backed creative portfolio…</div>';return}const r=state.research||{},p=state.portfolio||{},assets=p.assets||[],workers=state.workers||[];el.innerHTML=`<div style="display:flex;justify-content:space-between;gap:10px;align-items:flex-start"><div><div class="eyebrow" style="margin:0">RESEARCH + READY-TO-LAUNCH CREATIVE CMS</div><h2 style="font-size:18px;margin:5px 0">Creative Portfolio</h2><div class="brief">DOMINANCE researches search intent, Market Radar demand, competitor assets/changes and measured creative performance, then produces evidence-grounded copy, images, video and landing-page concepts.</div></div><button id="domRunCreativeResearch" class="btn primary">Research + Build Portfolio</button></div><div style="display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:7px;margin-top:10px"><div class="metric"><span>SEARCH INTENTS</span><b>${(r.keywords||[]).length}</b></div><div class="metric"><span>DEMAND EVENTS</span><b>${(r.market_events||[]).length}</b></div><div class="metric"><span>COMPETITOR ASSETS</span><b>${(r.competitor_assets||[]).length}</b></div><div class="metric"><span>COMPETITOR CHANGES</span><b>${(r.competitor_events||[]).length}</b></div><div class="metric"><span>MEASURED LEARNING</span><b>${(r.performance||[]).length}</b></div></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:10px"><div style="border:1px solid #20333e;border-radius:9px;padding:10px;background:#09161e"><b style="font-size:10px">Research reasoning inputs</b><div style="margin-top:6px;font-size:9px;color:#8195a3;line-height:1.5">${(r.keywords||[]).slice(0,6).map(x=>`<div>Search: <span style="color:#d9e8ed">${esc(x.phrase)}</span> · ${esc(x.dominant_intent||'unclassified')} · commerciality ${Math.round(Number(x.commerciality_score||0))}</div>`).join('')||'<div>No current search-intent evidence yet.</div>'}${(r.market_events||[]).slice(0,4).map(x=>`<div>Market: <span style="color:#d9e8ed">${esc(x.geography_name)}</span> · ${esc(x.event_type)} · score ${Math.round(Number(x.score||0))}</div>`).join('')}${(r.competitor_assets||[]).slice(0,4).map(x=>`<div>Competitor: <span style="color:#d9e8ed">${esc(x.competitor_name)}</span> · ${esc(x.platform)} · ${esc(x.message_angle||x.asset_type||'asset')}</div>`).join('')}</div></div><div style="border:1px solid #20333e;border-radius:9px;padding:10px;background:#09161e"><b style="font-size:10px">Worker status</b><div style="margin-top:6px;font-size:9px;color:#8195a3;line-height:1.5">${workers.map(w=>`<div>${esc(w.worker_key)} · <span style="color:${w.status==='completed'?'#5de0c2':'#f3ce62'}">${esc(w.status||'no_run')}</span></div>`).join('')||'<div>No creative worker runs recorded yet.</div>'}</div></div></div><div class="eyebrow" style="margin:14px 0 8px">GENERATED PORTFOLIO · ${assets.length} ASSETS</div><div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px">${assets.length?assets.map(assetCard).join(''):'<div style="grid-column:1/-1;border:1px solid #20333e;border-radius:9px;padding:18px;color:#8195a3">No generated assets yet. Once research and generation workers run, written copy, images, video jobs and landing-page concepts will appear here for approval.</div>'}</div>`;document.getElementById('domRunCreativeResearch')?.addEventListener('click',runResearch);el.querySelectorAll('[data-approve]').forEach(b=>b.addEventListener('click',()=>approve(b.dataset.approve)));el.querySelectorAll('[data-reject]').forEach(b=>b.addEventListener('click',()=>reject(b.dataset.reject)))}
 async function load(){try{state=await api('/api/creative-intelligence/library');render()}catch(e){ensure().innerHTML='<div class="eyebrow" style="margin:0">CREATIVE PORTFOLIO</div><div style="color:#ff9c9c;margin-top:8px;font-size:10px">'+esc(e.message)+'</div>'}}
 async function runResearch(){const b=document.getElementById('domRunCreativeResearch');if(b){b.disabled=true;b.textContent='Researching…'}try{await api('/api/creative-intelligence/research',{method:'POST',body:'{}'});await load()}catch(e){alert(e.message)}finally{if(b){b.disabled=false;b.textContent='Research + Build Portfolio'}}}
 async function approve(id){try{const d=await api('/api/creative-intelligence/assets/'+id+'/approve',{method:'POST',body:'{}'});alert(`Approved. ${d.creative_ids?.length||0} creative item(s) are now ready in Campaign Manager.`);await load()}catch(e){alert(e.message)}}
 async function reject(id){try{await api('/api/creative-intelligence/assets/'+id+'/reject',{method:'POST',body:'{}'});await load()}catch(e){alert(e.message)}}
 function start(){render();load();setInterval(load,60000)}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
