(function(){'use strict';function add(){try{if(typeof layers==='undefined')return;if(!layers.some(x=>x[0]==='competitive_pressure'))layers.push(['competitive_pressure','Competitive Pressure']);if(typeof renderLayers==='function')renderLayers()}catch(e){console.error('[MARKET MAP LAYERS]',e)}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',add,{once:true});else add()})();
