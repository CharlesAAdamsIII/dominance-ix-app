databases:
  - name: dominance-auth-db
    plan: starter
    databaseName: dominance
    user: dominance

services:
  - type: web
    name: dominance-market-radar
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      - key: MAX_CRAWL_PAGES
        value: 8
      - key: postgresql://dominance_db_user:O9CVYUL4NmEWc79VT0CqzBE3CpRGOvUX@dpg-daihosp5efls73dj8710-a/dominance_db
        fromDatabase:
          name: dominance-auth-db
          property: connectionString
      - key: 07eb25fb0644db3cfee06b28c8e4de92
        generateValue: true
