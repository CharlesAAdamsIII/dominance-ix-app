(function(){
  'use strict';
  const cfg=window.DOMINANCE_MAP_CONFIG||{};
  if(!cfg.enabled||!cfg.apiKey||!cfg.mapId)return;
  const GEO_CACHE_KEY='dominance_google_geo_cache_v1';
  const cache=(()=>{try{return JSON.parse(localStorage.getItem(GEO_CACHE_KEY)||'{}')}catch{return{}}})();
  const saveCache=()=>{try{localStorage.setItem(GEO_CACHE_KEY,JSON.stringify(cache))}catch{}};
  const finite=v=>Number.isFinite(Number(v));
  const clamp=(v,min=0,max=100)=>Math.max(min,Math.min(max,Number(v||0)));
  const norm=s=>String(s||'').trim().toLowerCase().replace(/\s+/g,' ');
  const rgb=v=>v>=85?[255,217,90]:v>=75?[243,184,79]:v>=65?[102,224,189]:v>=55?[56,183,178]:v>=40?[77,125,140]:[52,73,83];
  let gmap=null,overlay=null,geocoder=null,ready=false,geocodeBusy=false,pulsePhase=0,pulseTimer=null,googleLoadPromise=null,resizeObserver=null;
  let liveTelemetry={collection:{},sources:[],locations:[],hotspots:[]};
  function ensureGoogle(){if(window.google?.maps?.importLibrary)return Promise.resolve();if(googleLoadPromise)return googleLoadPromise;googleLoadPromise=new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://maps.googleapis.com/maps/api/js?key='+encodeURIComponent(cfg.apiKey)+'&loading=async&v=weekly';s.async=true;s.defer=true;s.onload=()=>window.google?.maps?.importLibrary?resolve():reject(Error('Google Maps API loaded without importLibrary.'));s.onerror=()=>reject(Error('Unable to load Google Maps JavaScript API.'));document.head.appendChild(s)});return googleLoadPromise}
  function marketCoordGoogle(m){if(finite(m?.latitude)&&finite(m?.longitude))return{lat:Number(m.latitude),lng:Number(m.longitude)};try{const c=marketCoord(m);if(c&&finite(c[0])&&finite(c[1]))return{lat:Number(c[0]),lng:Number(c[1])}}catch{}const mn=norm(m?.name),rn=norm(m?.region);for(const t of(state?.targets||[])){if(!t?.enabled||!finite(t.latitude)||!finite(t.longitude))continue;const tn=norm(t.name);if(tn===mn||tn.includes(mn)||mn.includes(tn)||(rn&&(tn.includes(rn)||rn.includes(tn))))return{lat:Number(t.latitude),lng:Number(t.longitude)}}for(const k of[mn,rn]){const c=cache[k];if(c&&finite(c.lat)&&finite(c.lng))return{lat:Number(c.lat),lng:Number(c.lng)}}return null}
  function liveLocation(m){const n=norm(m?.name);return(liveTelemetry.locations||[]).find(x=>norm(x.name)===n)||null}
  function freshnessScore(x){const age=Number(x?.age_seconds);if(!Number.isFinite(age))return 0;if(age<=3600)return 100;if(age<=21600)return 82;if(age<=86400)return 58;return 25}
  function metric(m){const key=typeof activeLayer==='string'?activeLayer:'score';const live=liveLocation(m);if(key==='live_collection')return freshnessScore(live);if(key==='demand_hotspots')return clamp(live?.hotspot_score??(Number(m?.search||0)*.65+Number(m?.velocity||0)*.2+Number(m?.confidence||0)*.15));if(key==='acceleration')return clamp(Math.max(Number(m?.velocity||0),live?.velocity||0));return clamp(m?.[key])}
  function radiusMeters(m,v){const type=String(m?.type||'').toLowerCase();const base=type==='city'?22000:type==='state'?90000:55000;return base+(v/100)*(type==='state'?140000:75000)}
  function selectedName(){try{return selected?.name||''}catch{return''}}
  function addGoogleLayers(){try{if(typeof layers==='undefined')return;const extras=[['live_collection','Live Collection'],['demand_hotspots','Demand Hotspots'],['acceleration','Acceleration']];for(const x of extras)if(!layers.some(y=>y[0]===x[0]))layers.unshift(x);if(typeof renderLayers==='function')renderLayers()}catch(e){console.warn('[GOOGLE MAP] layer controls',e)}}
  function setDiagnostics(mapped,total){let el=document.getElementById('domSpatialStatus');if(!el){el=document.createElement('div');el.id='domSpatialStatus';el.style.cssText='position:absolute;z-index:650;right:14px;bottom:16px;background:#071017e8;border:1px solid #5c4820;border-radius:8px;padding:8px 10px;font:9px Arial;color:#a9bdc7;line-height:1.45;pointer-events:none;max-width:270px';document.querySelector('.mapwrap')?.appendChild(el)}const layerName=(typeof layers!=='undefined'&&layers.find(x=>x[0]===activeLayer)?.[1])||activeLayer||'Opportunity',c=liveTelemetry.collection||{};el.innerHTML='<b style="color:#f3ce62">GOOGLE VECTOR · DECK.GL</b><br>'+mapped+'/'+total+' markets mapped · '+Number(c.observations_36h||0)+' observations / 36h<br>Layer: '+layerName+(mapped<total?'<br><span style="color:#ffd36d">Resolving '+(total-mapped)+' unmapped location'+(total-mapped===1?'':'s')+'…</span>':'')}
  function updateLegend(){const legend=document.getElementById('legend');if(!legend)return;const title=(typeof layers!=='undefined'&&layers.find(x=>x[0]===activeLayer)?.[1])||activeLayer||'Opportunity';let note='Heat = concentration · halo = opportunity · gold pulse = accelerating demand';if(activeLayer==='live_collection')note='Cyan = fresh automated observations · brighter = more recent';if(activeLayer==='demand_hotspots')note='Gold heat/rings = strongest current demand concentration';if(activeLayer==='acceleration')note='Gold pulse = fastest-rising demand geographies';legend.innerHTML='<b>'+title+'</b><div style="margin-top:5px;width:150px;height:7px;border-radius:8px;background:linear-gradient(90deg,#16394b,#1d777f,#36b9a7,#84d99a,#f3b84f,#ffd95a)"></div><div style="display:flex;justify-content:space-between;color:#8398a5;font-size:8px;margin-top:3px"><span>LOW</span><span>CONCENTRATED</span><span>HIGH</span></div><div style="margin-top:5px;color:#91a6b2;font-size:8px">'+note+'</div>'}
  function telemetryPoints(list){const out=[];for(const x of list||[]){const c=marketCoordGoogle({name:x.name,region:x.region,type:x.type});if(c)out.push({location:x,position:[c.lng,c.lat],value:activeLayer==='live_collection'?freshnessScore(x):activeLayer==='acceleration'?clamp(x.velocity):clamp(x.hotspot_score)})}return out}
  function renderGoogle(){
    if(!ready||!overlay||typeof deck==='undefined')return;
    const markets=(state?.markets||[]).slice(0,80),mapped=[];for(const m of markets){const c=marketCoordGoogle(m);if(c)mapped.push({m,c,v:metric(m)})}
    const livePts=telemetryPoints(liveTelemetry.locations),hotPts=telemetryPoints(liveTelemetry.hotspots);
    const displayPoints=activeLayer==='live_collection'?livePts:activeLayer==='demand_hotspots'?hotPts:mapped.map(x=>({position:[x.c.lng,x.c.lat],value:x.v,market:x.m}));
    const heatData=displayPoints.filter(x=>x.value>5).map(x=>({position:x.position,weight:Math.max(.05,x.value/100),market:x.market,location:x.location}));
    const points=mapped.map(x=>({position:[x.c.lng,x.c.lat],value:x.v,market:x.m}));
    const targets=(state?.targets||[]).filter(t=>t.enabled&&finite(t.latitude)&&finite(t.longitude)).map(t=>({position:[Number(t.longitude),Number(t.latitude)],radius:Number(t.radius_miles||25)*1609.344,target:t}));
    const highVelocity=(activeLayer==='acceleration'?telemetryPoints((liveTelemetry.locations||[]).filter(x=>Number(x.velocity)>=60)):points.filter(x=>Number(x.market?.velocity||0)>=65));
    const freshObservations=livePts.filter(x=>Number(x.location?.age_seconds)<=21600);
    const hotspots=hotPts.filter(x=>Number(x.location?.hotspot_score)>=60);
    const labels=(gmap?.getZoom?.()||4)>=5?(activeLayer==='demand_hotspots'?hotspots:points.filter(x=>x.value>=65)):[];
    const deckLayers=[
      new deck.HeatmapLayer({id:'dom-demand-heat',data:heatData,getPosition:d=>d.position,getWeight:d=>d.weight,radiusPixels:(gmap?.getZoom?.()||4)<=4?58:38,intensity:activeLayer==='demand_hotspots'?1.8:1.35,threshold:.02,colorRange:[[22,57,75],[29,119,127],[54,185,167],[132,217,154],[243,184,79],[255,217,90]]}),
      new deck.ScatterplotLayer({id:'dom-market-halos',data:activeLayer==='live_collection'?[]:points,getPosition:d=>d.position,getRadius:d=>radiusMeters(d.market,d.value),radiusUnits:'meters',filled:true,stroked:true,getFillColor:d=>[...rgb(d.value),26],getLineColor:d=>[...rgb(d.value),d.value>=72?210:110],lineWidthMinPixels:1,getLineWidth:d=>d.value>=72?2:1,pickable:true,onClick:info=>{if(info.object?.market)selectMarket(info.object.market,true)}}),
      new deck.ScatterplotLayer({id:'dom-live-observations',data:freshObservations,getPosition:d=>d.position,getRadius:d=>22000*(1+pulsePhase*.22),radiusUnits:'meters',radiusMinPixels:5,radiusMaxPixels:18,filled:true,stroked:true,getFillColor:[93,224,194,55],getLineColor:[93,224,194,220],lineWidthMinPixels:2,pickable:false}),
      new deck.ScatterplotLayer({id:'dom-hotspot-rings',data:hotspots,getPosition:d=>d.position,getRadius:d=>(45000+Number(d.location.hotspot_score||0)*1100)*(1+pulsePhase*.12),radiusUnits:'meters',radiusMinPixels:12,filled:true,stroked:true,getFillColor:[243,184,79,42],getLineColor:d=>Number(d.location.hotspot_score)>=80?[255,217,90,245]:[243,184,79,210],lineWidthMinPixels:3,pickable:false}),
      new deck.ScatterplotLayer({id:'dom-market-points',data:points,getPosition:d=>d.position,getRadius:d=>d.market?.name===selectedName()?9000:6000,radiusUnits:'meters',radiusMinPixels:5,radiusMaxPixels:14,filled:true,stroked:true,getFillColor:d=>[...rgb(d.value),230],getLineColor:d=>d.market?.name===selectedName()?[255,255,255,255]:[12,24,31,220],lineWidthMinPixels:2,pickable:true,onClick:info=>{if(info.object?.market)selectMarket(info.object.market,true)}}),
      new deck.ScatterplotLayer({id:'dom-target-radii',data:targets,getPosition:d=>d.position,getRadius:d=>d.radius,radiusUnits:'meters',filled:true,stroked:true,getFillColor:d=>d.target.source==='user'?[243,206,98,20]:[93,224,194,16],getLineColor:d=>d.target.source==='user'?[243,206,98,230]:[93,224,194,200],lineWidthMinPixels:2,pickable:false}),
      new deck.ScatterplotLayer({id:'dom-velocity-pulse',data:highVelocity,getPosition:d=>d.position,getRadius:d=>(28000+Number(d.market?.velocity||d.location?.velocity||0)*650)*(1+pulsePhase*.38),radiusUnits:'meters',filled:false,stroked:true,getLineColor:[255,217,90,Math.round(180*(1-pulsePhase*.55))],lineWidthMinPixels:2,pickable:false}),
      new deck.TextLayer({id:'dom-market-labels',data:labels,getPosition:d=>d.position,getText:d=>d.location?('HOTSPOT · '+Math.round(d.location.hotspot_score||0)):String(d.market.name)+' · '+Math.round(d.value),getSize:11,sizeUnits:'pixels',getColor:[236,247,251,235],getTextAnchor:'middle',getAlignmentBaseline:'bottom',background:true,getBackgroundColor:[7,16,23,205],backgroundPadding:[4,2],pickable:false})
    ];
    overlay.setProps({layers:deckLayers,getTooltip:({object})=>object?.market?`${object.market.name}\n${Math.round(object.value||metric(object.market))}/100 · ${object.market.decision||''}`:object?.location?`${object.location.name}\nHotspot ${Math.round(object.location.hotspot_score||0)}/100 · ${object.location.freshness||''}`:null});
    setDiagnostics(mapped.length,markets.length);updateLegend();queueMarketGeocodes(markets.filter(m=>!marketCoordGoogle(m)).slice(0,8));
  }
  async function geocodeMarket(m){const key=norm(m?.name);if(!key||cache[key]||!geocoder)return false;try{const response=await geocoder.geocode({address:String(m.name)+(m.region&&norm(m.region)!==key?', '+m.region:''),componentRestrictions:{country:'US'}});const r=response.results?.[0];if(r){cache[key]={lat:r.geometry.location.lat(),lng:r.geometry.location.lng(),name:r.formatted_address,at:Date.now()};saveCache();return true}}catch(e){console.warn('[GOOGLE MAP] market geocode failed',m?.name,e.message)}cache[key]={failed:true,at:Date.now()};saveCache();return false}
  async function queueMarketGeocodes(list){if(geocodeBusy||!list.length)return;geocodeBusy=true;let changed=false;for(const m of list){const c=cache[norm(m.name)];if(c?.failed&&Date.now()-Number(c.at||0)<86400000)continue;if(await geocodeMarket(m))changed=true;await new Promise(r=>setTimeout(r,180))}geocodeBusy=false;if(changed)renderGoogle()}
  async function googleSearchLocation(){const q=$('locationSearch').value.trim();if(!q||!geocoder)return;$('locationResults').innerHTML='<div class="empty">Searching Google…</div>';try{const response=await geocoder.geocode({address:q,componentRestrictions:{country:'US'}});const results=(response.results||[]).slice(0,5).map(r=>({name:r.formatted_address,latitude:r.geometry.location.lat(),longitude:r.geometry.location.lng(),type:r.types?.[0]||'location',category:'google_geocoding'}));window._geo=results;$('locationResults').innerHTML=results.map((x,i)=>`<div class="result" onclick="chooseLocation(${i})">${esc(x.name)}</div>`).join('')||'<div class="empty">No U.S. location found.</div>'}catch(e){$('locationResults').innerHTML='<div class="empty">'+esc(e.message||'Google location lookup failed.')+'</div>'}}
  function patchUi(){addGoogleLayers();try{drawMap=renderGoogle}catch{}try{selectMarket=function(m,fly){selected=m;renderSelected();renderGoogle();const c=marketCoordGoogle(m);if(fly&&c&&gmap){gmap.panTo(c);gmap.setZoom(String(m?.type||'').toLowerCase()==='city'?7:5)}}}catch{}try{searchLocation=googleSearchLocation}catch{}try{const b=document.getElementById('profileBadge');if(b)b.title='Google Maps vector renderer with deck.gl live intelligence overlays'}catch{}}
  window.DOMINANCE_GOOGLE_MAP={setTelemetry(d){liveTelemetry=d||{collection:{},sources:[],locations:[],hotspots:[]};renderGoogle()},render:renderGoogle,isReady:()=>ready,getMap:()=>gmap};
  async function init(){try{await ensureGoogle();if(typeof deck==='undefined'||!deck.GoogleMapsOverlay)throw Error('deck.gl Google Maps bundle did not initialize.');const {Map}=await google.maps.importLibrary('maps');const {Geocoder}=await google.maps.importLibrary('geocoding');geocoder=new Geocoder();try{if(typeof map!=='undefined'&&map&&typeof map.remove==='function')map.remove()}catch{}const el=document.getElementById('map');if(!el)throw Error('Market map container missing.');el.innerHTML='';el.className='map';gmap=new Map(el,{center:{lat:38.5,lng:-96},zoom:4,mapId:cfg.mapId,tilt:0,heading:0,gestureHandling:'greedy',mapTypeControl:false,streetViewControl:false,fullscreenControl:true,zoomControl:true,clickableIcons:false});overlay=new deck.GoogleMapsOverlay({interleaved:true,layers:[]});overlay.setMap(gmap);ready=true;patchUi();gmap.addListener('zoom_changed',()=>setTimeout(renderGoogle,40));gmap.addListener('heading_changed',()=>setTimeout(renderGoogle,40));gmap.addListener('tilt_changed',()=>setTimeout(renderGoogle,40));if(window.ResizeObserver){resizeObserver=new ResizeObserver(()=>{if(!ready)return;try{google.maps.event.trigger(gmap,'resize')}catch{}setTimeout(renderGoogle,30)});resizeObserver.observe(document.querySelector('.mapwrap')||el)}pulseTimer=setInterval(()=>{pulsePhase=(pulsePhase+.18)%1;renderGoogle()},1400);setTimeout(renderGoogle,0);const scan=document.getElementById('scanText');if(scan)scan.textContent=(scan.textContent||'DOMINANCE Market Radar')+' · Google vector map active · live telemetry loading';console.log('[DOMINANCE] Google Maps vector + deck.gl live intelligence renderer active')}catch(e){console.error('[DOMINANCE] Google Maps migration failed; Leaflet fallback remains available.',e);const scan=document.getElementById('scanText');if(scan)scan.textContent='Google map unavailable; using DOMINANCE fallback map. '+(e.message||'')}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,0),{once:true});else setTimeout(init,0);
})();
